master · 1 changed file · last commit "wizard: machine name first, grouped phase output, SSH-only master key with key titles, done/skip choices; clone falls back to the remote default branch; picker ignores group names; template identity commented; installers captured; -q after subcommand honored"
CLAUDE.md
no summary: no session transcript for this project
